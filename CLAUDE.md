# CLAUDE.md

이 문서는 Snap-Sight의 개발 규칙, 아키텍처 제약, 팀 간 인터페이스 계약을 담는다. 각 역할(①~⑥) 담당자가 자신의 인터페이스를 확정하는 대로 이 문서에 섹션을 추가해나가는 걸 제안한다. 아래는 ⑥(Android — UX/피드백)이 이슈 #5 작업 중 정리한 부분이다.

## ②↔⑥ 인터페이스 계약 (⑥ 작성, 2026-08-14 기준)

편차 관련 원시 값은 ②(온디바이스 CV, 이슈 #2)가 계산하고, 판정(카테고리 분류)은 ⑥이 담당한다. 상세 근거와 미확정 항목은 `docs/interfaces/cv-to-ux-interface-draft.md`를 참고한다.

### 확정된 사항
- ②는 판정 카테고리가 아니라 원시 값(dx)만 제공한다
- 거리(크기 비율)는 당장 계산하지 않는다
- LOST/AMBIGUOUS는 당장 하나의 탐지 실패 상태로 합쳐 처리한다
- ②→⑥ 전달은 같은 앱 프로세스 내부 호출로 추정된다 (② 본인도 확신도가 낮아 완전히 확정된 것은 아님)

### 아직 확정되지 않은 사항
- dy(수직 편차) 반영 여부와 시점
- 실제 갱신 주기 (현재는 PC 기준 수치만 있고, 안드로이드 기기 기준 수치는 없음)
- 실제 구현 언어 (Kotlin 여부 미확정)

GuidanceState 필드 정의와 임계값은 `docs/ux/guidance-state-schema.md`, 상태별 피드백 매핑은 `docs/ux/feedback-mapping.md`, TalkBack 동작은 `docs/ux/talkback-behavior.md`를 참고한다.

## 다른 섹션

①③④⑤ 역할의 인터페이스 계약은 각 담당자가 추가할 예정이다.
