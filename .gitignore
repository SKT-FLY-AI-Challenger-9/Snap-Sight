CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.runtime/

# Local databases
*.sqlite3
*.db

# Environment variables / secrets
.env
.env.local
*.key
*.pem

# Model files / training artifacts
*.pt
*.tflite
*.mlmodel
*.onnx
*.h5
*.ckpt
runs/
wandb/

# ultralytics export 가 남기는 중간 산출물 (.pt → onnx → SavedModel → tflite)
*_saved_model/
*.pb

# ② CV 온디바이스 모델 자산만 예외 — Android assets 에 들어가야 앱이 로드할 수 있다
!frontend/app/src/main/assets/*.tflite

# Image/buffer cache (test capture data)
*.jpg
*.jpeg
*.png
!docs/**/*.png
frame_buffer/
captures/
demo_outputs/

# Local voice test recordings (personal audio, not for commit)
ai/samples/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
*.log

# Mobile (Android build artifacts)
build/
.gradle/
.cxx/
*.apk
*.aab

# Android — 로컬 SDK 경로·모듈 파일 (머신마다 다름, 커밋 금지)
local.properties
*.iml

# Android — 서명 키 (절대 커밋 금지)
*.jks
*.keystore

# Virtual Environment
.venv/
