// 이 파일: Figma 시안(#80, Make v31)의 다크 테마 공통 색상 — 모든 화면이 같은 팔레트를 쓴다.
// 값은 시안 웹 프리뷰의 computed style에서 그대로 추출했다 (iOS 시스템 팔레트 계열).
package com.example.snap_sight.ux

import androidx.compose.ui.graphics.Color

object SnapPalette {
    val Background = Color(0xFF0D0D0D)
    val Card = Color(0xFF1C1C1E)
    val CardBorder = Color(0xFF2C2C2E)
    val InputBorder = Color(0xFF3A3A3C)
    val Accent = Color(0xFF0A84FF)
    val AccentLight = Color(0xFF5AC8FA)
    val AccentSoft = Color(0xFF0A2540)
    val TextPrimary = Color(0xFFF5F5F5)
    val TextSecondary = Color(0xFFB0B0B6)
    val TextTertiary = Color(0xFFD1D1D6)
    val Success = Color(0xFF30D158)
    val Warning = Color(0xFFFFD60A)
    val WarningStrong = Color(0xFFFF9F0A)
}
